[project]
name = "venture-autopsy-backend"
version = "1.0.0"
description = "Multi-Agent Enterprise Due Diligence Platform — Backend"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
